# agent-policy

> Pure-function policy matrix for AI coding agents.
> Map `(repo, capability, context)` to `deny` / `require_approval` / `auto_allow`.

**Status**: 0.1.0 alpha — public API frozen, examples pending.

See `ARCHITECTURE.md` (internal) for the design rationale and what is *not* in scope.
Full README with examples lands before the v0.1.0 PyPI publish.
